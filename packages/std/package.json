{
  "name": "@drawmygame/std",
  "version": "1.0.0",
  "catalogname": "std",
  "description": "DrawMyGame standard catalog",
  "main": "dist/v1/index.mjs",
  "type": "module",
  "scripts": {
    "build": "babel src -d dist --copy-files --extensions .mjs --out-file-extension .mjs --config-file ../../.babelrc && cp -r static/* dist/ 2>/dev/null || true",
    "postinstall": "npm run build"
  },
  "author": "nicolas.carrez@gmail.com",
  "license": "ISC",
  "dependencies": {
    "@drawmygame/core": "^1.0.0"
  }
}
